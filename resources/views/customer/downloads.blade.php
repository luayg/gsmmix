@extends(auth()->check() ? 'layouts.customer' : 'layouts.site')
@section('title','Downloads')
@section('content')
<section class="{{ auth()->check()?'':'container py-5' }}"><span class="eyebrow">DOWNLOAD CENTER</span><h1 class="h2 fw-bold">Files, tools and resources</h1><p class="text-muted mb-4">The latest resources published by our team.</p><div class="row g-3">@forelse($downloads as $download)<div class="col-md-6"><article class="service-catalog-card d-flex gap-3"><div class="service-icon flex-shrink-0"><i class="fas fa-file-arrow-down"></i></div><div class="flex-grow-1"><div class="d-flex justify-content-between gap-2"><h2 class="h5 fw-bold mb-1">{{ $download->name }}</h2><span class="badge {{ $download->is_free?'text-bg-success':'text-bg-primary' }}">{{ $download->is_free?'Free':'Members' }}</span></div><p class="text-muted small mb-2">{{ Str::limit($download->description,130) }}</p><div class="small text-muted">{{ $download->version ? 'Version '.$download->version : 'Latest version' }} · {{ number_format(($download->size ?? 0)/1048576,1) }} MB</div></div></article></div>@empty<div class="col-12"><div class="empty-state"><i class="fas fa-cloud-arrow-down"></i><h2>No downloads available</h2><p>Published resources will appear here.</p></div></div>@endforelse</div><div class="mt-4">{{ $downloads->links() }}</div></section>
@endsection
