@extends('layouts.customer') @section('title','Transactions') @section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="section-title h2">Payment history</h1><p class="text-muted mb-0">Track automatic and manually reviewed deposits.</p></div><a class="btn btn-gsm" href="{{ route('customer.payments.create') }}">Add funds</a></div><div class="gsm-card p-3"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Reference</th><th>Method</th><th>Amount</th><th>Fee</th><th>Status</th><th>Date</th></tr></thead><tbody>@forelse($transactions as $row)<tr><td><a href="{{ route('customer.payments.show',$row) }}">{{ str($row->uuid)->limit(12) }}</a></td><td>{{ $row->gateway?->name ?? 'Historical method' }}</td><td>{{ $row->payable_currency }} {{ $row->currency_code }}</td><td>{{ $row->fee_base }}</td><td><span class="status-pill status-{{ $row->status }}">{{ ucfirst($row->status) }}</span></td><td>{{ $row->created_at->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No payments yet.</td></tr>@endforelse</tbody></table></div>{{ $transactions->links() }}</div>@endsection
