@extends('layouts.admin') @section('title','Backups') @section('content')<div class="card"><div class="card-header bg-primary text-white">Encrypted database backups</div><div class="card-body">@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif<form method="POST" class="mb-3">@csrf<button class="btn btn-primary">Create backup</button></form><table class="table"><tr><th>File</th><th>Size</th><th>Created</th><th></th></tr>@foreach($files as $f)<tr><td>{{ $f['name'] }}</td><td>{{ number_format($f['size']/1024,1) }} KB</td><td>{{ date('Y-m-d H:i',$f['modified']) }}</td><td><a class="btn btn-sm btn-success" href="{{ route('admin.system.backups.download',$f['name']) }}">Download</a><form class="d-inline" method="POST" action="{{ route('admin.system.backups.destroy',$f['name']) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></td></tr>@endforeach</table><div class="alert alert-warning">Restore is intentionally unavailable from the web panel and requires a verified operator procedure.</div></div></div>@endsection
