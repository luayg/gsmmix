@extends('layouts.admin') @section('title','File manager') @section('content')<div class="card"><div class="card-header bg-primary text-white">Private file manager</div><div class="card-body"><div class="alert alert-info">Access is restricted to <code>storage/app/private/system-files</code>. Executable and browser-active files are blocked.</div>@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif<form method="POST" enctype="multipart/form-data" class="d-flex gap-2 mb-3">@csrf<input class="form-control" type="file" name="file" required><button class="btn btn-primary">Upload</button></form><table class="table"><tr><th>Name</th><th>Size</th><th>Modified</th><th></th></tr>@foreach($files as $f)<tr><td>{{ $f['name'] }}</td><td>{{ number_format($f['size']/1024,1) }} KB</td><td>{{ date('Y-m-d H:i',$f['modified']) }}</td><td><a class="btn btn-sm btn-success" href="{{ route('admin.system.filemanager.download',$f['name']) }}">Download</a><form class="d-inline" method="POST" action="{{ route('admin.system.filemanager.destroy',$f['name']) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></td></tr>@endforeach</table></div></div>@endsection
