@extends('layouts.admin') @section('title','System update') @section('content')<div class="card"><div class="card-header bg-primary text-white">System readiness</div><div class="card-body">@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif<table class="table"><tr><th>PHP</th><td>{{ $php }}</td></tr><tr><th>Laravel</th><td>{{ $laravel }}</td></tr><tr><th>Commit</th><td><code>{{ $commit ?: 'Unavailable' }}</code></td></tr></table><form method="POST" action="{{ route('admin.system.update.cache') }}">@csrf<button class="btn btn-outline-primary">Clear application caches</button></form><div class="alert alert-warning mt-3">Arbitrary ZIP upload and web-executed code updates are disabled. Releases must be signed, backed up, and deployed through the trusted release pipeline.</div></div></div>@endsection
