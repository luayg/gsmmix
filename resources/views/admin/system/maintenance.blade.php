@extends('layouts.admin') @section('title','Maintenance') @section('content')<div class="card"><div class="card-header bg-warning">Maintenance mode</div><div class="card-body">@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif<form method="POST">@csrf @method('PUT')<input type="hidden" name="enabled" value="0"><label><input type="checkbox" name="enabled" value="1" @checked($enabled)> Enable maintenance notice</label><textarea class="form-control my-3" name="message" placeholder="Public maintenance message"></textarea><button class="btn btn-warning">Save</button></form><p class="text-muted mt-3">Administrators remain able to access this panel. This flag is ready for the future customer middleware.</p></div></div>@endsection
