<div class="table-responsive"><table class="table report-table align-middle mb-0"><thead><tr><th>Date</th><th>Type</th><th>Reference</th><th>Amount</th><th>Left amount</th><th>Note</th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $row->created_at->format('Y-m-d H:i:s') }}</td><td><span class="report-badge {{ $row->direction==='income'?'is-income':'is-expense' }}">{{ $row->direction==='income'?'Income':'Expense' }}</span></td><td><strong>{{ $row->reference ?: str($row->kind)->replace('_',' ')->title() }}</strong></td><td class="{{ $row->direction==='income'?'text-success':'text-danger' }}">{{ $row->direction==='income'?'+':'-' }}{{ number_format((float)$row->amount,4) }}</td><td class="report-money">{{ number_format((float)$row->balance_after,4) }}</td><td>{{ $row->note ?: str($row->kind)->replace('_',' ')->title() }}</td></tr>@empty<tr><td colspan="6" class="empty-state">No transactions in this period.</td></tr>@endforelse</tbody></table></div>
