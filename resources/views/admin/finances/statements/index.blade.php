@extends('layouts.admin')
@section('title','Statements')
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">FINANCE</span><h1>Customer statements</h1><p>Balances, total receipts, used credit, locked funds and overdraft in one report.</p></div></div>
<div class="card admin-panel"><div class="card-body"><form class="report-filters"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Customer name or email"><input class="form-control" name="user_id" type="number" value="{{ request('user_id') }}" placeholder="Customer ID"><button class="btn btn-primary">Filter</button><a class="btn btn-light" href="{{ route('admin.finances.statements.index') }}">Reset</a></form></div><div class="table-responsive"><table class="table report-table align-middle mb-0"><thead><tr><th>Customer</th><th>Current balance</th><th>Total receipts</th><th>Paid credits</th><th>Locked amount</th><th>Overdraft</th><th></th></tr></thead><tbody>@forelse($accounts as $account)<tr><td><strong>{{ $account->user?->name ?? 'Historical account' }}</strong><small>{{ $account->user?->email }} · ID {{ $account->user_id }}</small></td><td class="report-money">{{ number_format((float)($account->user?->balance ?? 0),4) }}</td><td>{{ number_format((float)$account->total_receipts,2) }}</td><td>{{ number_format((float)$account->paid_credits,2) }}</td><td>{{ number_format((float)$account->locked_amount,2) }}</td><td>{{ number_format((float)$account->overdraft_limit,2) }}</td><td><a class="btn btn-sm btn-primary" href="{{ route('admin.finances.statements.show',$account->user_id) }}">Open statement</a></td></tr>@empty<tr><td colspan="7" class="empty-state">No accounts match.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white">{{ $accounts->links() }}</div></div>
@endsection
