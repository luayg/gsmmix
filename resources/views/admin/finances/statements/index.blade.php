@extends('layouts.admin')
@section('title','Statements')
@section('content')<div class="card"><div class="card-header bg-primary text-white">Customer statements</div><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-4"><input class="form-control" name="user_id" type="number" value="{{ request('user_id') }}" placeholder="Customer ID"></div><div class="col"><button class="btn btn-primary">Filter</button></div></form><div class="table-responsive"><table class="table table-striped"><thead><tr><th>Customer</th><th>Balance</th><th>Receipts</th><th>Paid credits</th><th>Locked</th><th>Overdraft</th><th></th></tr></thead><tbody>@forelse($accounts as $account)<tr><td>{{ $account->user?->name ?? 'Historical account' }} #{{ $account->user_id }}</td><td>{{ $account->user?->balance ?? 0 }}</td><td>{{ $account->total_receipts }}</td><td>{{ $account->paid_credits }}</td><td>{{ $account->locked_amount }}</td><td>{{ $account->overdraft_limit }}</td><td><a class="btn btn-sm btn-primary" href="{{ route('admin.finances.statements.show',$account->user_id) }}">Open statement</a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted">No accounts match.</td></tr>@endforelse</tbody></table></div>{{ $accounts->links() }}</div></div>@endsection
