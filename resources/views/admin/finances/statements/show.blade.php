@extends('layouts.admin')
@section('title','Statement - '.$user->name)
@section('content')<div class="card"><div class="card-header bg-primary text-white d-flex justify-content-between"><strong>Statement: {{ $user->name }}</strong><span><a class="btn btn-light btn-sm" href="{{ route('admin.finances.statements.print',[$user]+request()->query()) }}" target="_blank">Print</a> <a class="btn btn-light btn-sm" href="{{ route('admin.finances.statements.export',[$user]+request()->query()) }}">CSV</a></span></div><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-3"><input class="form-control" type="date" name="from" value="{{ request('from') }}"></div><div class="col-md-3"><input class="form-control" type="date" name="to" value="{{ request('to') }}"></div><div class="col"><button class="btn btn-primary">Apply</button></div></form><div class="row mb-3"><div class="col">Opening: <strong>{{ number_format((float)$opening,4) }}</strong></div><div class="col">Credits: <strong>{{ number_format((float)$summary['income'],4) }}</strong></div><div class="col">Debits: <strong>{{ number_format((float)$summary['expense'],4) }}</strong></div><div class="col">Refunds: <strong>{{ number_format((float)$summary['refund'],4) }}</strong></div></div>@include('admin.finances.statements.table',['rows'=>$transactions]){{ $transactions->links() }}</div></div>@endsection
