@extends('layouts.admin')
@section('title','Statement · '.$user->name)
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">ACCOUNT STATEMENT</span><h1>{{ $user->name }}</h1><p>{{ $user->email }} · Customer #{{ $user->id }}</p></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" target="_blank" href="{{ route('admin.finances.statements.print',[$user]+request()->query()) }}"><i class="fas fa-print me-1"></i>Print</a><a class="btn btn-primary" href="{{ route('admin.finances.statements.export',[$user]+request()->query()) }}"><i class="fas fa-file-csv me-1"></i>CSV</a></div></div>
<div class="statement-summary"><div><span>Opening balance</span><strong>{{ number_format((float)$opening,4) }}</strong></div><div><span>Total income</span><strong class="text-success">+{{ number_format((float)$summary['income'],4) }}</strong></div><div><span>Total expense</span><strong class="text-danger">-{{ number_format((float)$summary['expense'],4) }}</strong></div><div><span>Refunds</span><strong>{{ number_format((float)$summary['refund'],4) }}</strong></div><div><span>Current balance</span><strong>{{ number_format((float)$user->balance,4) }}</strong></div></div>
<div class="card admin-panel"><div class="card-body"><form class="report-filters"><input class="form-control" type="date" name="from" value="{{ request('from') }}"><input class="form-control" type="date" name="to" value="{{ request('to') }}"><button class="btn btn-primary">Apply</button><a class="btn btn-light" href="{{ route('admin.finances.statements.show',$user) }}">Reset</a></form></div>@include('admin.finances.statements.table',['rows'=>$transactions])<div class="card-footer bg-white">{{ $transactions->links() }}</div></div>
@endsection
