@extends('layouts.admin')
@section('title','Invoices')
@section('content')<div class="card"><div class="card-header bg-primary text-white d-flex justify-content-between"><strong>Invoices</strong><a class="btn btn-light btn-sm" href="{{ route('admin.finances.invoices.create') }}">Create invoice</a></div><div class="card-body">@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif<form class="row g-2 mb-3"><div class="col-md-5"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Invoice number or customer"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(\App\Models\Invoice::STATUSES as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str_replace('_',' ',ucfirst($status)) }}</option>@endforeach</select></div><div class="col"><button class="btn btn-primary">Filter</button></div></form><div class="table-responsive"><table class="table table-striped"><thead><tr><th>Number</th><th>Customer</th><th>Status</th><th>Issued</th><th>Due</th><th>Total</th><th>Paid</th><th>Balance</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td><a href="{{ route('admin.finances.invoices.show',$invoice) }}">{{ $invoice->number }}</a></td><td>{{ $invoice->user?->name }}</td><td><span class="badge bg-secondary">{{ $invoice->status }}</span></td><td>{{ $invoice->issued_at?->format('Y-m-d') }}</td><td>{{ $invoice->due_at?->format('Y-m-d') }}</td><td>{{ $invoice->total }} {{ $invoice->currency_code }}</td><td>{{ $invoice->paid_total }}</td><td>{{ $invoice->balance_due }}</td></tr>@empty<tr><td colspan="8" class="text-center text-muted">No invoices match.</td></tr>@endforelse</tbody></table></div>{{ $invoices->links() }}</div></div>@endsection
