@extends('layouts.admin')
@section('title','Invoices')
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">FINANCE</span><h1>Payment invoices</h1><p>Search, review, print and track every issued customer invoice.</p></div><a class="btn btn-primary" href="{{ route('admin.finances.invoices.create') }}"><i class="fas fa-plus me-1"></i>Create invoice</a></div>
@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif
<div class="card admin-panel"><div class="card-body"><form class="report-filters"><input class="form-control" type="date" name="from" value="{{ request('from') }}"><input class="form-control" type="date" name="to" value="{{ request('to') }}"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Invoice number or customer"><select class="form-select" name="user_id"><option value="">All customers</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected((string)request('user_id')===(string)$user->id)>{{ $user->email }} — {{ $user->name }}</option>@endforeach</select><select class="form-select" name="status"><option value="">All types</option>@foreach(\App\Models\Invoice::STATUSES as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ str($status)->replace('_',' ')->title() }}</option>@endforeach</select><button class="btn btn-primary">Filter</button><a class="btn btn-light" href="{{ route('admin.finances.invoices.index') }}">Reset</a></form></div>
<div class="table-responsive"><table class="table report-table align-middle mb-0"><thead><tr><th>Date</th><th>Invoice ID</th><th>Amount</th><th>Tax</th><th>Fee</th><th>Customer</th><th>Type</th><th>Actions</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td>{{ $invoice->created_at->format('Y-m-d H:i') }}</td><td><strong>{{ $invoice->number }}</strong></td><td>{{ number_format((float)$invoice->total,2) }} {{ $invoice->currency_code }}</td><td>{{ number_format((float)$invoice->tax_total,2) }}</td><td>{{ number_format((float)$invoice->fee_total,2) }}</td><td>{{ $invoice->user?->email }}<small>{{ $invoice->user?->name }}</small></td><td><span class="report-badge is-{{ $invoice->status }}">{{ str($invoice->status)->replace('_',' ')->title() }}</span></td><td><a class="btn btn-sm btn-primary" href="{{ route('admin.finances.invoices.show',$invoice) }}">View</a> <a class="btn btn-sm btn-outline-primary" target="_blank" href="{{ route('admin.finances.invoices.print',$invoice) }}"><i class="fas fa-print"></i></a></td></tr>@empty<tr><td colspan="8" class="empty-state">No invoices match the selected filters.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white">{{ $invoices->links() }}</div></div>
@endsection
