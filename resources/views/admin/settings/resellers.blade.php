@extends('layouts.admin')
@section('title','Resellers')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="h4 mb-1">Top-up resellers</h1><p class="text-muted mb-0">Manage agents customers can contact to fund their accounts.</p></div><button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addReseller"><i class="fas fa-plus me-1"></i>Add reseller</button></div>@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Reseller</th><th>Country</th><th>Phone</th><th>WhatsApp</th><th>Status</th><th>Order</th><th></th></tr></thead><tbody>@forelse($resellers as $r)<tr><td><div class="d-flex align-items-center gap-2">@if($r->image_path)<img src="{{ asset('storage/'.$r->image_path) }}" class="rounded-circle" style="width:42px;height:42px;object-fit:cover">@endif<strong>{{ $r->name }}</strong></div></td><td>{{ $r->country ?: '—' }}</td><td>{{ $r->phone }}</td><td>{{ $r->whatsapp ?: '—' }}</td><td>{{ $r->active?'Active':'Inactive' }}</td><td>{{ $r->ordering }}</td><td><button class="btn btn-sm btn-warning" data-bs-toggle="modal" data-bs-target="#editReseller{{ $r->id }}">Edit</button> <form class="d-inline" method="POST" action="{{ route('admin.settings.resellers.destroy',$r) }}" onsubmit="return confirm('Delete reseller?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">No resellers yet.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $resellers->links() }}</div>
@include('admin.settings.reseller-modal',['modalId'=>'addReseller','reseller'=>new \App\Models\Reseller,'action'=>route('admin.settings.resellers.store'),'method'=>'POST'])
@foreach($resellers as $reseller)@include('admin.settings.reseller-modal',['modalId'=>'editReseller'.$reseller->id,'reseller'=>$reseller,'action'=>route('admin.settings.resellers.update',$reseller),'method'=>'PUT'])@endforeach
@endsection
