@extends('layouts.admin') @section('title','Download categories') @section('content')<div class="card"><div class="card-header bg-primary text-white">Download categories</div><div class="card-body">@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('admin.downloads.categories.store') }}" class="row g-2 mb-4">@csrf<div class="col"><input class="form-control" name="name" placeholder="Category name" required></div><div class="col"><input class="form-control" name="description" placeholder="Description"></div><div class="col"><input class="form-control" name="required_permission" placeholder="Optional permission"></div><div class="col-1"><input class="form-control" type="number" name="ordering" value="0"></div><div class="col-auto"><input type="hidden" name="active" value="0"><label><input type="checkbox" name="active" value="1" checked> Active</label></div><div class="col-auto"><button class="btn btn-primary">Add</button></div></form><table class="table table-striped"><thead><tr><th>Name</th><th>Description</th><th>Permission</th><th>Order</th><th>Files</th><th></th></tr></thead><tbody>@foreach($categories as $category)<tr><form method="POST" action="{{ route('admin.downloads.categories.update',$category) }}">@csrf @method('PUT')<td><input class="form-control" name="name" value="{{ $category->name }}"></td><td><input class="form-control" name="description" value="{{ $category->description }}"></td><td><input class="form-control" name="required_permission" value="{{ $category->required_permission }}"></td><td><input class="form-control" type="number" name="ordering" value="{{ $category->ordering }}"><input type="hidden" name="active" value="0"><label><input type="checkbox" name="active" value="1" @checked($category->active)> active</label></td><td>{{ $category->downloads_count }}</td><td><button class="btn btn-sm btn-warning">Save</button></form> <form class="d-inline" method="POST" action="{{ route('admin.downloads.categories.destroy',$category) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-danger">Delete</button></form></td></tr>@endforeach</tbody></table>{{ $categories->links() }}</div></div>@endsection
