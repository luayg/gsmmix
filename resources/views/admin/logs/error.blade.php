@extends('layouts.admin')
@section('title','Error logs')
@section('content')
<div class="admin-page-head"><div><span class="admin-kicker">SYSTEM HEALTH</span><h1>Application error logs</h1><p>Search recent Laravel errors with sensitive credentials automatically redacted.</p></div></div>
<div class="card admin-panel"><div class="card-body"><form class="report-filters"><select class="form-select" name="file">@foreach($files as $file)<option value="{{ $file['name'] }}" @selected($selected===$file['name'])>{{ $file['name'] }} · {{ number_format($file['size']/1024,1) }} KB</option>@endforeach</select><select class="form-select" name="level"><option value="">All levels</option>@foreach(['emergency','alert','critical','error','warning','notice','info','debug'] as $level)<option @selected(request('level')===$level)>{{ $level }}</option>@endforeach</select><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Search error content"><button class="btn btn-primary">Filter</button></form></div><div class="table-responsive"><table class="table report-table align-middle mb-0"><thead><tr><th>Level</th><th>Date</th><th>Content</th></tr></thead><tbody>@forelse($rows as $row)<tr><td><span class="report-badge is-expense">{{ strtoupper($row['level']) }}</span></td><td class="text-nowrap">{{ $row['date'] }}</td><td><details><summary class="error-summary">{{ Str::limit(strtok($row['content'],"\n"),220) }}</summary><pre class="error-content">{{ $row['content'] }}</pre></details></td></tr>@empty<tr><td colspan="3" class="empty-state">No parsed errors match this log and filter.</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-white">{{ $rows->links() }}</div></div>
@endsection
