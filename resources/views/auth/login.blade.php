@extends('layouts.guest')
@section('title','Sign in')
@section('content')
<div class="auth-shell"><div class="auth-visual"><a class="brand-mark" href="{{ route('home') }}">GSM<span>MIX</span></a><div><span class="eyebrow text-info">WELCOME BACK</span><h1>Your GSM workspace, always ready.</h1><p>Track orders, manage funds and access every service from one streamlined dashboard.</p></div><div class="auth-trust"><span><i class="fas fa-shield-halved"></i> Secure</span><span><i class="fas fa-bolt"></i> Fast</span><span><i class="fas fa-globe"></i> Global</span></div></div><div class="auth-form"><div class="w-100" style="max-width:460px"><h2 class="fw-bold">Sign in to your account</h2><p class="text-muted">Enter your email or username to continue.</p>@if(session('ok'))<div class="alert alert-success">{{ session('ok') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ url('/login') }}">@csrf<div class="mb-3"><label class="form-label">Email or username</label><input class="form-control form-control-lg" name="login" value="{{ old('login') }}" required autofocus autocomplete="username webauthn"></div><div class="mb-3"><label class="form-label">Password</label><input type="password" class="form-control form-control-lg" name="password" required autocomplete="current-password"></div><div class="form-check mb-4"><input class="form-check-input" type="checkbox" name="remember" id="remember"><label class="form-check-label" for="remember">Keep me signed in</label></div><button class="btn btn-primary btn-lg w-100">Sign in securely</button></form><div class="text-center text-muted my-3">or</div><button type="button" class="btn btn-outline-primary btn-lg w-100" data-passkey-login><i class="fas fa-key me-2"></i>Sign in with a passkey</button><div data-passkey-login-message hidden></div>@if(app(\App\Services\Settings\AppSettings::class)->get('general.google_login_enabled',false) && app(\App\Services\Settings\AppSettings::class)->get('auth.google_client_id') && app(\App\Services\Settings\AppSettings::class)->get('auth.google_client_secret'))<a class="btn btn-outline-dark btn-lg w-100 mt-3" href="{{ route('auth.google') }}"><i class="fab fa-google me-2"></i> Continue with Google</a>@endif @if(app(\App\Services\Settings\AppSettings::class)->get('general.registration_enabled',false))<p class="text-center text-muted mt-4">New to GSMMIX? <a href="{{ route('register') }}">Create an account</a></p>@endif</div></div></div>
@endsection
