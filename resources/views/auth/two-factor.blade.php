@extends('layouts.guest')
@section('title','Two-step verification')
@section('content')
<div class="auth-shell"><div class="auth-visual"><a class="brand-mark" href="{{ route('home') }}">GSM<span>MIX</span></a><div><span class="eyebrow text-info">SECURITY CHECK</span><h1>Confirm it is really you.</h1><p>We sent a six-digit code to your account email. The code expires in ten minutes.</p></div></div><div class="auth-form"><div class="w-100" style="max-width:460px"><h2 class="fw-bold">Two-step verification</h2><p class="text-muted">Enter the verification code sent to your email.</p>@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('two-factor.verify') }}">@csrf<div class="mb-4"><label class="form-label">Verification code</label><input inputmode="numeric" autocomplete="one-time-code" pattern="[0-9]{6}" maxlength="6" class="form-control form-control-lg text-center" name="code" required autofocus></div><button class="btn btn-primary btn-lg w-100">Verify and sign in</button></form><p class="text-center mt-4"><a href="{{ route('login') }}">Return to sign in</a></p></div></div></div>
@endsection
