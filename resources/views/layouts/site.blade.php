<!doctype html><html lang="{{ str_replace('_','-',app()->getLocale()) }}" dir="{{ app()->getLocale()==='ar'?'rtl':'ltr' }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>@yield('title',config('app.name','GSMMIX'))</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body>
@php($routeFor=['home'=>'home','services'=>'site.services','store'=>'site.store','downloads'=>'site.downloads','place-order'=>'customer.orders.create','orders'=>'customer.orders','account'=>'customer.dashboard'])
<nav class="navbar navbar-expand-lg navbar-dark site-nav fixed-top"><div class="container-fluid px-lg-5"><a class="brand-mark" href="{{ route('home') }}">GSM<span>MIX</span></a><button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#siteNav"><span class="navbar-toggler-icon"></span></button><div class="collapse navbar-collapse" id="siteNav"><ul class="navbar-nav mx-auto gap-lg-2">@foreach($headerPages as $navPage)@php($target=$routeFor[$navPage->slug]??null)@if($target)<li class="nav-item"><a class="nav-link" href="{{ route($target) }}">{{ $navPage->translations->first()?->title ?? ucfirst($navPage->slug) }}</a></li>@else<li class="nav-item"><a class="nav-link" href="{{ route('site.page',$navPage) }}">{{ $navPage->translations->first()?->title ?? ucfirst($navPage->slug) }}</a></li>@endif @endforeach</ul><div class="d-flex gap-2">@auth<a class="btn btn-gsm" href="{{ route('customer.dashboard') }}">Dashboard</a><form method="POST" action="{{ route('logout') }}">@csrf<button class="btn btn-outline-light">Logout</button></form>@else<a class="btn btn-outline-light" href="{{ route('login') }}">Login</a><a class="btn btn-gsm" href="{{ route('register') }}">Create account</a>@endauth</div></div></div></nav><main>@yield('content')</main>
<footer class="site-footer py-5"><div class="container"><div class="row g-4"><div class="col-lg-5"><a class="brand-mark" href="{{ route('home') }}">GSM<span>MIX</span></a><p class="mt-3">Professional GSM services, secure payments and reliable global support.</p></div><div class="col-lg-7"><div class="d-flex flex-wrap gap-4 justify-content-lg-end">@foreach($footerPages as $navPage)<a href="{{ route('site.page',$navPage) }}">{{ $navPage->translations->first()?->title ?? ucfirst($navPage->slug) }}</a>@endforeach</div></div></div><hr class="border-secondary"><div class="small">© {{ date('Y') }} {{ config('app.name','GSMMIX') }}. All rights reserved.</div></div></footer>@stack('scripts')</body></html>
